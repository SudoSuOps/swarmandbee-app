# Swarm & Bee · Order Book Shim · environment
#
# Rename this to `.env` (the real .env is .gitignored). The same key MUST be
# set in CF Pages → Settings → Environment Variables → ORDERBOOK_API_KEY.

ORDERBOOK_API_KEY=replace-me-with-a-long-random-token
